# 印向未来 · 改版进度记录

> 线上地址：https://ogy36000-svg.github.io/print-career/
> 更新机制：改 `data/*.json` → 推送 main 分支 → 网站刷新即生效；改代码 → `npm run build` → `out/` 推送 gh-pages（详见文末「部署备忘」）

---

## ✅ 已完成（2026-09-09，已上线并浏览器验证通过）

- [x] 部署修复：Vercel SSO 锁死 → 迁移 GitHub Pages
- [x] CMYK 四色墨点加载动画（用户好评）
- [x] 首页中国地图点位涌现动画（SVG 省份轮廓版，动画效果好评，但地图本身要换真实地图API → 见待办）
- [x] 企业库扩充至 60 家（出版社/印钞/国企/设备商），设备/产能/市值字段
- [x] 职业百科 8 岗位（好坏对照+职业健康）
- [x] 行业趋势页（内卷/出海航线图/印刷英语）
- [x] 考证中心：倒计时自动计算、官方公布/按往届推算标注、官网直达（好评）
- [x] 成长中心闯关路径（想法好评，展现形式要改 → 见待办）
- [x] 简历上传本地解析（PDF/TXT）
- [x] 招聘平台 6 个直达跳转（好评）
- [x] 收藏/投递状态追踪（好评）
- [x] 数据热更新架构（GitHub 作内容库，lib/remote.ts）

---

## 🔨 待办清单（2026-09-10 用户反馈整理，按优先级）

### P0 内容/结构硬伤
1. **首页标题重写** —— 「选专业，看企业在地图上冒出来」被否，换简洁有力的（DJI 风格）
2. **专业可自由输入** —— 不限定选项，保留数字印刷等快捷项 + 任意输入框
3. **内容随专业联动** —— 企业/职业百科/趋势/考证 按所选专业过滤（majors.json 加 relatedRoles/relatedCerts 字段）；未知专业显示「数据定制中」兜底
4. **企业卡片加官网链接** —— 卡片上直接可点进官网；卡片背景换成行业实拍风图片（用图片生成API按类别生成：数码车间/包装厂/出版社/印钞等）；删掉「为什么推荐给你」这类 AI 味文案，改「企业亮点」
5. **文案去AI味** —— 删「查它在招什么（实时）」等，全部改成面向用户的中性表达
6. **趋势页可跳转** —— 每条趋势挂真实新闻源链接（keyin.cn 等），卡片可点击；印刷英语模块降级为可折叠

### P1 地图与布局
7. **换真实地图 API** —— Leaflet + 高德瓦片（免Key），可缩放拖动，点位精确；加载失败降级为现有 SVG 地图；出海模块同样换真实世界地图（Leaflet 同一组件缩放实现）
8. **企业详情页布局重排** —— 桌面端左右分栏（左主内容/右信息栏），消除两侧大留白；职业健康提示移到右侧栏、弱化配色
9. **成长中心改横向** —— 树状上下 → 从左到右横向时间轴（桌面横排可横滑/移动端竖排紧凑）；位移小、信息密度高
10. **成长页布局去留白** —— 左右加看板：左=考证倒计时+匹配企业榜，右=目标企业追踪+档案摘要；档案摘要并入成长页
11. **技能可自定义输入** —— 技能/证书/意向全部支持手动输入添加，不只点选固定项

### P2 新功能
12. **右下角书信式建议** —— 浮动信封按钮，点开是一封「信」（根据档案生成的个性化建议，温暖纸感设计，内容取自 实习就业规划建议.md）
13. **职业百科主次重构** —— 按类别分组、重点岗位大卡+次要小卡，顶部类别导航，KPI 条（薪资/门槛/环境）

### P3 远期方向（需要更大改造，记录在这里）
- 任意专业（如建筑工程）的**实时内容生成**：需要接 AI 接口做服务端生成，当前架构支持「跟我说一声→我生成数据→推送即生效」的半自动模式
- 企业官网实拍图自动抓取（60家官网图片爬取）
- Supabase 建表后开启云端同步（用户尚未建表）

---

## 部署备忘（本机网络坑）
- Node fetch 被本地代理拦截 → 用 PowerShell Invoke-RestMethod 调 API
- git push 大包失败 → `git config http.version HTTP/1.1` + `http.postBuffer 524288000`
- `npm run build` 会清空 out/（含 .git/.nojekyll）→ `.nojekyll` 已放 public/ 自动拷贝；out/.git 每次重建后重新 init
- 凭据在 `../secrets.local.txt`
